apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: dwoproject
  name: app
spec:
  ports:
    - name: "80"
      port: 80
      targetPort: 80
  selector:
    run: dwoproject
status:
  loadBalancer: {}
